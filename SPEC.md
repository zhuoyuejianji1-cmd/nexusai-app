# AI 学习社区 - 项目规范

## 1. 概念与愿景

**NexusAI** — 一个充满活力的 AI 学习社区，将 AI 知识探索变成一场令人兴奋的旅程。它不仅仅是学习平台，更是一个 AI 时代的"知识星球"，用户可以在这里追踪 AI 前沿动态、分享学习心得、完成成长任务。整体体验如同使用一款精心设计的科技产品——流畅、智能、令人愉悦。

---

## 2. 设计语言

### 美学方向
**科技极简主义 + 动态光效**：深色主题为主，搭配流动的渐变光效和微妙的几何图形，营造出未来感十足的 AI 氛围。界面保持克制，让内容成为主角。

### 色彩系统
```css
:root {
  /* 主色 */
  --primary: #6366F1;           /* 靛蓝紫 - 代表智慧与创新 */
  --primary-light: #818CF8;
  --primary-dark: #4F46E5;

  /* 强调色 */
  --accent: #22D3EE;            /* 青色 - 代表技术光芒 */
  --accent-glow: rgba(34, 211, 238, 0.15);

  /* 背景 */
  --bg-primary: #09090B;        /* 深黑背景 */
  --bg-secondary: #18181B;      /* 卡片背景 */
  --bg-tertiary: #27272A;       /* 悬浮/选中 */
  --bg-elevated: #3F3F46;        /* 边框等 */

  /* 文字 */
  --text-primary: #FAFAFA;
  --text-secondary: #A1A1AA;
  --text-muted: #71717A;

  /* 状态 */
  --success: #10B981;
  --warning: #F59E0B;
  --error: #EF4444;

  /* 渐变 */
  --gradient-primary: linear-gradient(135deg, #6366F1 0%, #8B5CF6 50%, #22D3EE 100%);
  --gradient-card: linear-gradient(145deg, rgba(99, 102, 241, 0.1) 0%, rgba(34, 211, 238, 0.05) 100%);
}
```

### 字体
- **主字体**：Inter (正文、UI)
- **标题字体**：Space Grotesk (标题、数字、强调)
- **中文**：Noto Sans SC

### 空间系统
- 基础单位：4px
- 间距序列：4, 8, 12, 16, 24, 32, 48, 64, 96
- 圆角：sm(6px), md(12px), lg(16px), xl(24px), full(9999px)
- 卡片阴影：`0 0 40px rgba(99, 102, 241, 0.08)`

### 动效哲学
- **入场动画**：元素从下方 20px 淡入上滑，duration 400ms，ease-out
- **悬浮效果**：transform scale(1.02)，box-shadow 增强
- **加载状态**：骨架屏 + 脉冲光效
- **过渡**：所有过渡 200ms ease-out
- **微交互**：按钮点击缩放 0.98，点赞心跳动画

### 视觉资产
- **图标**：Lucide Icons（线性风格，stroke-width: 1.5）
- **图片策略**：Unsplash 科技/抽象主题，用户头像默认渐变色块 + 首字母
- **装饰元素**：流动渐变光斑、点阵背景、同心圆纹理

---

## 3. 布局与结构

### 整体架构
```
┌─────────────────────────────────────────────────────────────┐
│  顶部导航栏 (固定)                                            │
│  Logo | 首页 | 资源 | 学习 | [搜索框] | 头像/登录             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  主内容区 (最大宽度 1280px，居中)                             │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  首页动态流                                            │  │
│  │  ┌──────────────┬──────────────────────────────┐      │  │
│  │  │ 侧边栏        │ 主内容                      │      │  │
│  │  │ - AI热榜      │ - AI一周大事 (轮播)          │      │  │
│  │  │ - 快捷入口    │ - 精选文章                  │      │  │
│  │  │              │ - 用户动态流                 │      │  │
│  │  └──────────────┴──────────────────────────────┘      │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  资源区                                               │  │
│  │  分类标签 + 搜索 + 资源卡片网格                        │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  学习中心                                             │  │
│  │  今日任务卡片 + 成长路径 + 学习档案                    │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 响应式策略
- **桌面 (≥1024px)**：三栏布局，侧边栏 + 主内容 + 推荐
- **平板 (768-1023px)**：两栏，侧边栏折叠为图标
- **移动端 (<768px)**：单栏，底部 Tab 导航

---

## 4. 功能与交互

### 4.1 首页动态流

#### AI 一周大事
- 横向轮播展示本周最重要的 5 条 AI 新闻
- 每条包含：标题、摘要、来源标签、时间
- 悬浮显示"阅读更多"按钮
- 自动轮播 5s/张，支持手动切换

#### AI 热榜
- 实时更新的 AI 工具/话题热度排行
- Top 10 显示，支持展开更多
- 每个条目：排名、名称、热度指数、趋势箭头
- 点击跳转到相关资源/文章

#### 精选文章
- 瀑布流布局，2-3 列
- 每篇文章卡片：封面图、标题、摘要、作者、点赞数、阅读量
- 悬浮放大效果，点击进入详情页

#### 用户动态流
- 实时更新的用户分享列表
- 每条动态：用户头像、昵称、内容、图片（可选）、时间、点赞评论操作
- **发布动态**：点击"发布"按钮弹出编辑器
- **点赞**：心形图标，点击动画 + 数字变化
- **评论**：气泡图标，点击展开评论列表，输入框支持发送

### 4.2 资源区

#### 分类浏览
- 顶部标签栏：全部 | AI工具 | 教程 | 论文 | 视频 | 数据集 | 开源项目
- 点击标签筛选，支持多选

#### 搜索功能
- 支持关键词搜索
- 实时搜索建议（下拉列表）
- 搜索历史记录
- 空搜索显示热门推荐

#### 资源卡片
- 封面图/图标
- 资源名称、简介
- 分类标签、来源
- 收藏数、浏览数
- 悬浮显示"查看详情"和"收藏"按钮

### 4.3 学习中心

#### 每日任务
- 突出展示今日学习任务
- 任务卡片：任务名称、描述、预计时长、奖励积分
- **开始学习**：点击进入任务详情
- **任务详情页**：
  - 任务目标说明
  - 分步骤指导（可折叠展开）
  - 相关资源链接
  - "完成任务"按钮
- 完成打卡：填写学习心得/截图
- 连续打卡显示徽章

#### 成长路径
- 可视化路径图（从起点到终点的节点连接）
- 路径节点：入门 → 基础 → 进阶 → 实战 → 专家
- 每个节点显示：名称、描述、完成状态、所需任务数
- 点击节点查看该阶段的学习任务

#### 学习档案
- 个人学习数据统计
- 学习天数、完成任务数、获得积分
- 学习时长趋势图（近7天）
- 已获得徽章展示
- 学习偏好标签

### 4.4 个人中心

#### 用户信息
- 头像、昵称、个人简介
- 编辑资料按钮

#### 我的动态
- 用户发布的动态列表
- 支持编辑/删除

#### 我的收藏
- 收藏的资源列表
- 支持取消收藏

#### 学习记录
- 完成任务历史
- 打卡日历

---

## 5. 组件清单

### 全局组件

#### 导航栏 (Navbar)
- **默认**：深色背景，Logo 左侧，导航链接居中，功能按钮右侧
- **悬浮**：链接下方出现 2px 高亮条
- **激活**：文字变为主色调
- **移动端**：汉堡菜单，侧滑抽屉

#### 卡片 (Card)
- **默认**：圆角 16px，微渐变背景，细边框
- **悬浮**：上浮 4px，阴影增强，轻微发光
- **加载**：骨架屏占位

#### 按钮 (Button)
- **Primary**：渐变背景，白色文字
- **Secondary**：透明背景，主色边框和文字
- **Ghost**：透明背景，悬浮显示背景色
- **禁用**：50% 透明度，禁用光标
- **加载**：文字替换为旋转图标

#### 输入框 (Input)
- **默认**：深色背景，细边框
- **聚焦**：边框变主色，外发光
- **错误**：红色边框，错误提示文字
- **搜索框**：左侧搜索图标，右侧清空按钮

#### 标签 (Tag)
- 小圆角，药丸形状
- 多种颜色：主色、成功、警告、灰色
- 可关闭（显示 x 按钮）

#### 头像 (Avatar)
- 圆形，默认显示渐变色块 + 首字母
- 可配置大小：sm(32px), md(40px), lg(56px)
- 支持在线状态指示点

#### 模态框 (Modal)
- 居中显示，背景模糊遮罩
- 淡入缩放动画
- 右上角关闭按钮
- 支持自定义标题、内容、底部操作

#### 消息提示 (Toast)
- 顶部居中弹出
- 成功（绿色）、警告（黄色）、错误（红色）、信息（蓝色）
- 3 秒后自动消失，可手动关闭

### 业务组件

#### 新闻轮播 (NewsCarousel)
- 全宽卡片式轮播
- 底部指示点，当前高亮
- 左右箭头（悬浮显示）
- 自动轮播 + 手动切换

#### 热榜列表 (HotList)
- 数字排名（1-3 特殊样式）
- 名称 + 热度条 + 趋势
- 悬浮整行高亮

#### 动态卡片 (PostCard)
- 用户信息区：头像、昵称、时间
- 内容区：文字 + 图片网格
- 操作区：点赞、评论、分享
- 评论展开区

#### 资源卡片 (ResourceCard)
- 左侧图标/封面
- 右侧信息区
- 底部标签和统计数据
- 悬浮显示操作按钮

#### 任务卡片 (TaskCard)
- 状态指示：待完成(灰)、进行中(蓝)、已完成(绿)
- 进度条（如果有子任务）
- 奖励标签
- CTA 按钮

#### 路径节点 (PathNode)
- 圆形节点 + 连接线
- 完成状态：实心 + 勾选
- 进行中：脉冲动画
- 未完成：空心

---

## 6. 技术方案

### 技术栈
- **框架**：Next.js 16 (App Router)
- **语言**：TypeScript 5
- **UI 组件**：shadcn/ui
- **样式**：Tailwind CSS 4
- **数据库**：Supabase (PostgreSQL)
- **状态管理**：React Context + useState

### 数据库模型

#### users (用户表)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| email | text | 邮箱 |
| nickname | text | 昵称 |
| avatar_url | text | 头像URL |
| bio | text | 个人简介 |
| points | integer | 积分 |
| created_at | timestamp | 创建时间 |

#### posts (动态表)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| user_id | uuid | 用户ID |
| content | text | 内容 |
| images | jsonb | 图片URL数组 |
| likes_count | integer | 点赞数 |
| comments_count | integer | 评论数 |
| created_at | timestamp | 创建时间 |

#### likes (点赞表)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| user_id | uuid | 用户ID |
| post_id | uuid | 动态ID |
| created_at | timestamp | 创建时间 |

#### comments (评论表)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| user_id | uuid | 用户ID |
| post_id | uuid | 动态ID |
| content | text | 评论内容 |
| created_at | timestamp | 创建时间 |

#### resources (资源表)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| title | text | 标题 |
| description | text | 描述 |
| category | text | 分类 |
| url | text | 链接 |
| cover_url | text | 封面图 |
| likes_count | integer | 收藏数 |
| views_count | integer | 浏览数 |
| created_at | timestamp | 创建时间 |

#### tasks (任务表)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| title | text | 任务名称 |
| description | text | 描述 |
| steps | jsonb | 步骤JSON |
| duration | text | 预计时长 |
| points | integer | 奖励积分 |
| path_id | integer | 所属路径 |
| created_at | timestamp | 创建时间 |

#### user_task_progress (用户任务进度)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| user_id | uuid | 用户ID |
| task_id | uuid | 任务ID |
| status | text | 状态 |
| completed_at | timestamp | 完成时间 |
| notes | text | 学习心得 |

#### news (AI新闻表)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| title | text | 标题 |
| summary | text | 摘要 |
| source | text | 来源 |
| url | text | 原文链接 |
| week_number | integer | 所属周 |
| created_at | timestamp | 创建时间 |

#### hot_list (热榜表)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| name | text | 名称 |
| category | text | 分类 |
| heat_score | integer | 热度分数 |
| trend | text | 趋势 |
| created_at | timestamp | 创建时间 |

### API 设计

#### 动态相关
- `GET /api/posts` - 获取动态列表 (分页)
- `POST /api/posts` - 发布动态
- `DELETE /api/posts/[id]` - 删除动态
- `POST /api/posts/[id]/like` - 点赞
- `DELETE /api/posts/[id]/like` - 取消点赞
- `GET /api/posts/[id]/comments` - 获取评论
- `POST /api/posts/[id]/comments` - 添加评论

#### 资源相关
- `GET /api/resources` - 获取资源列表 (支持分类筛选、搜索)
- `GET /api/resources/[id]` - 获取资源详情
- `POST /api/resources/[id]/like` - 收藏

#### 任务相关
- `GET /api/tasks/today` - 获取今日任务
- `GET /api/tasks/paths` - 获取成长路径
- `POST /api/tasks/[id]/complete` - 完成任务

#### 新闻/热榜
- `GET /api/news/weekly` - 获取本周新闻
- `GET /api/hot-list` - 获取热榜

### 路由结构
```
/                    - 首页动态流
/resources           - 资源区
/learn               - 学习中心
/learn/task/[id]     - 任务详情
/profile             - 个人中心
/profile/edit        - 编辑资料
/api/*               - API 路由
```

---

## 7. 实现优先级

### P0 - 核心功能
1. 项目初始化 + UI 基础组件
2. 首页布局 + 导航
3. 动态列表展示
4. 发布动态功能
5. 资源列表 + 搜索
6. 任务列表 + 完成流程

### P1 - 增强功能
7. 点赞/评论功能
8. 用户档案
9. 热榜 + 新闻轮播
10. 成长路径可视化

### P2 - 优化体验
11. 加载状态优化
12. 空状态设计
13. 错误处理
14. 响应式适配
