# 开发指南

## 编码规范

### TypeScript/React代码
1. **命名规范**:
   - 组件使用PascalCase（如 `ResourceLogo`）
   - 变量和函数使用camelCase（如 `handleLikePost`）
   - 接口使用PascalCase（如 `HotListItem`）
   - 常量使用UPPER_SNAKE_CASE（如 `ZHIPU_API_KEY`）

2. **注释要求**:
   - 每个函数必须有中文注释说明用途
   - 复杂逻辑必须有行内注释
   - 接口和类型必须有注释说明

3. **错误处理**:
   - 所有API调用必须有try-catch
   - 错误信息使用中文
   - 失败时提供用户友好的提示

### Python代码
1. **命名规范**:
   - 类名使用PascalCase（如 `YoyoSpider`）
   - 函数和变量使用snake_case（如 `get_article_list`）
   - 常量使用UPPER_SNAKE_CASE（如 `REQUEST_TIMEOUT`）

2. **注释要求**:
   - 每个函数必须有中文docstring
   - 参数和返回值必须有说明
   - 复杂逻辑必须有行内注释

## 开发流程

### 添加新功能

1. **确定需求**: 明确功能目标和用户场景
2. **设计模块**: 确定文件位置、数据结构、UI布局
3. **实现功能**: 编写代码，遵循现有代码风格
4. **测试功能**: 在浏览器中测试，确保功能正常
5. **更新文档**: 更新PROGRESS.md和相关文档

## 常见问题排查

### 热榜点击无反应

**症状**: 点击热榜条目无法跳转

**排查步骤**:
1. 检查Link组件是否有正确的href
2. 检查是否有CSS遮挡（z-index问题）
3. 检查子元素是否有pointer-events-none

**解决方案**:
- 给Link添加 `relative z-10`
- 给子元素添加 `pointer-events-none`

### 热榜详情显示"未找到"

**症状**: 点击热榜显示"未找到该热榜内容"

**排查步骤**:
1. 检查数据文件路径是否正确
2. 检查API返回的数据结构
3. 检查ID是否匹配

**解决方案**:
- 统一使用API获取数据，不要直接访问JSON文件

### 课程内容包含无用信息

**症状**: 课程详情中出现价格、时间、下载链接等

**排查步骤**:
1. 检查爬虫的内容清洗逻辑
2. 检查无用关键词列表

**解决方案**:
- 在clean_content函数中添加更多过滤规则

## 调试技巧

### 日志查看
- 使用 `console.log` 打印关键数据
- 使用浏览器开发者工具查看网络请求

### 断点调试
- 使用 `debugger` 语句设置断点
- 使用浏览器开发者工具的Sources面板

## 性能优化

### 图片加载
- 使用懒加载（loading="lazy"）
- 优化图片尺寸
- 使用WebP格式

### 数据加载
- 使用分页减少初始加载数据
- 使用静态数据减少API调用
- 使用缓存减少重复请求

## 安全注意事项

1. 不要在代码中硬编码密钥
2. 使用环境变量存储敏感信息
3. API路由验证用户身份
4. 爬虫使用延迟避免被封IP
5. 不暴露用户敏感信息
